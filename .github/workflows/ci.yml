name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1

    - name: Install
      run: |
        sudo apt-get install graphviz
        cp .env.dist .env
        composer install
        npm install

    - name: Test
      run: |
        composer test
        npm test
